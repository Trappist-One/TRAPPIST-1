server:
  port: 8100
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: job-admin
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 112.74.62.174:8848
        file-extension: yaml
        group: local
        namespace: local
        username: nacos
        password: t@nacos
        shared-configs[0]:
          data-id: job-admin.yaml
          group: LOCAL
          refresh: true
